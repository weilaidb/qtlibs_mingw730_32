#include <cstdlib>$NL#include <iostream>$NL#include <time.h>$NL#include <stdio.h>$NL $NLusing namespace std;$NL$NL//说明，localtime只会保留最后一份数据，如果在多线程中使用，会存在覆盖的问题，不是线程安全的$NL$NLint \1()$NL{$NL    time_t tNow =time(NULL);$NL    time_t tEnd = tNow + 1800;$NL    //注意下面两行的区别$NL    struct tm* ptm = localtime(&tNow);$NL    struct tm* ptmEnd = localtime(&tEnd);$NL $NL    char szTmp[50] = {0};$NL    strftime(szTmp,50,"%H:%M:%S",ptm);$NL    char szEnd[50] = {0};$NL    strftime(szEnd,50,"%H:%M:%S",ptmEnd);$NL    $NL $NL    printf("%s /n",szTmp);$NL    printf("%s /n",szEnd);$NL    $NL $NL    system("PAUSE");$NL    return EXIT_SUCCESS;$NL}