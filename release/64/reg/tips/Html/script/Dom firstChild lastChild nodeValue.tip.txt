<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>To-Do List firstChild lastChild</title>
    <script>
        function countListItem(){
            var olElement = document.getElementById("toDoList");
            var count = 0;
            // alert("olElement length:" + olElement.childNodes.length);
            for(var i = 0;i < olElement.childNodes.length;i++){
                if(olElement.childNodes[i].nodeType ===1){
                    count++;
                }
            }
            alert("The ordered list contains " + count + " items");
            var firstChild = olElement.childNodes[0];
            var lastChild = olElement.childNodes[olElement.childNodes.length-1];
            var lastChildNode = olElement.lastChild;
            var parentElement = olElement.lastChild.parentNode;

            var firstOne = olElement.firstChild;
            var nextOne = firstOne.nextSibling;

            alert(firstChild);
            alert(lastChild);
            alert(lastChildNode);
            alert(parentElement);

        }


        function nodeValue(){
            var text = "";
            var pElement = document.getElementById("toDoNotes");
            for(var i = 0;i < pElement.childNodes.length;i++){
                text += pElement.childNodes[i].nodeValue;
            }
            alert("The paragraph says:\n\n" + text);
        }

        // window.onload = countListItem;
        window.onload = nodeValue;
    </script>
</head>
<body>
<h1>
    Dom 文档对象类型
    Things To Do
</h1>
<ol id="toDoList">
    <li>Move the lawn</li>
    <li>Clean the windows</li>
    <li>Answer your email</li>
</ol>

<p id="toDoNotes">Make sure all thse are completed by 8pm so you can watch the game on TV!</p>
</body>
<script>
    var olElement = document.getElementById("toDoList");
    //遍历
    var count = 0;
    for(var i = 0; i < olElement.childNodes.length;i++){
        if(olElement.childNodes[i].nodeType === 1){
            continue;
        }else{

        }
    }
</script>
</html>