Hybrid接口
能够接收和发送多个VLAN的数据，可以用于连接交换机之间的链路，也可以用于连接交换机与终端设备。

与Trunk接口的区别
相同点:Trunk接口和Hybrid接口在接收入站数据时，处理方法是相同的。
不同点:在发送出站数据时，Trunk接口只会摘除PVID标签。也就是说，当数据所有VLAN为该Trunk接口的缺省VLAN时，Trunk接口才会去掉VLAN标签并进行转发；对于其它VLAN的数据，Trunk接口在转发时不会摘除相应的VLAN标签。
而Hybrid接口能够以不携带VLAN标签的方式发送多个VLAN的数据。