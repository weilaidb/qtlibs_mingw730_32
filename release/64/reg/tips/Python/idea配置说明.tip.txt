库存放的位置:
web/WEB-INF/lib下

Project Structure下配置
Project Settings -- Libraries -- lib -- 添加
classes和sources分别添加对应的classes和lib
G:\tarbao\jsp\HelloJavaEE3\web\WEB-INF\classes
G:\tarbao\jsp\HelloJavaEE3\web\WEB-INF\lib


websocket-api.jar在对应的Tomcat/lib下就有，拷贝一份过来
WebSocket 作为java的一个扩展，它属于javax包目录下，通常需要手工引入该jar，以tomcat为例，可以在 tomcat/lib 目录下找到 websocket-api.jar