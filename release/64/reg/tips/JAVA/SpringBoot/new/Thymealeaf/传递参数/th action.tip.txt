===========================

<form th:action="@{/user/login}" th:method="post">
    <input type="text" name="username" id="username" value="weiwei"/>
    <input type="text" name="password" id="password" value="weiwei"/>
    <input type="submit" value="提交"/>
</form>



login.html
<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>登录界面</title>
</head>
<body>
<!--th:text 为 Thymeleaf 属性，用于在展示文本-->
<h1 th:text="${message}">欢迎您访问静态页面 HTML</h1>

</body>
</html>




main.html
<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>主界面</title>
</head>
<body>
<!--th:text 为 Thymeleaf 属性，用于在展示文本-->
<h1 th:text="主界面">主界面 HTML</h1>
</body>
</html>




======================
package com.weilaidb.nice.controller;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Controller;
import org.springframework.util.StringUtils;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestParam;

import java.util.Map;

@Controller
public class LoginController {

    @PostMapping("user/login")
    public String login(Map<String, Object> map,
                        @RequestParam("username") String username,
                        @RequestParam("password") String password) {
        Logger logger = LoggerFactory.getLogger(getClass());

        if (!StringUtils.isEmpty(username)&&"123".equals(password)){
            logger.error("checkright");
            return "redirect:/main";
        }
        else {
            logger.error("error 用户名或密码错误");
            map.put("message","用户名或密码错误");
            return "login";
        }
    }
}






##需要创建Controller从指定templates下对应的哪个html文件
1.在templates下创建hello.html,内容仿照上面的
2.创建Controller，指定html的名称，示例2
3.传递参数使用HttpServletRequest


问题列表:
1./访问地址 报错
--html写的有问题
--原来就无法正常访问