===========================
<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Title</title>

    <script>

        function getInfo(){
            alert("this is info");
        }

        function inputinfo(info){
            alert("this is info" + info);
        }

    </script>
</head>
<body>
<!--th:text 为 Thymeleaf 属性，用于在展示文本-->
<h1 th:text="迎您来到Thymeleaf">欢迎您访问静态页面 HTML</h1>


<input  id="html-id"  th:id="thymeleaf-id"  />

<h1 th:text="hello，bianchengbang">hello</h1>

<div th:text="'<h1>欢迎来到编程帮！</h1>'" >欢迎你</div>
<div th:utext="'<h1>欢迎来到编程帮！</h1>'" >欢迎你(utext是富文本)</div>


<!--<div th:object="${session.user}" >-->
<!--/*@thymesVar id="user" type=""*/-->
<div th:object="${session.user}" >
    <p th:text="*{fisrtName}">firstname</p>
</div>



<input th:value = "${user.name}" />


<div th:with="isEvens = ${prodStat.count}%2 == 0"  th:text="${isEvens}"></div>
<div th:with="isOdd = ${prodStat.count}%2 != 0"  th:text="${isOdd}"></div>

<div th:style="'color:#F00; font-weight:bold'">编程帮 www.biancheng.net</div>


<td th:onclick = "'getInfo()'"></td>


<button class="btn" th:onclick="getInfo()">获得名字</button>
<button class="btn" th:onclick="inputinfo([[${prodStat.count}]])">传递参数</button>
<!--存在问题-->
<button class="btn" th:onclick="inputinfo(${prodStat.count})">传递参数2</button>



<!--//在form请求部分要写上th:object="${equManager}"-->
<!--//<form class="form form-horizontal"  id="form-equManager-add" action="#" //th:action="@{/distribution/equManager/add}"-->
<!--//th:object="${equManager}"  enctype="multipart/form-data">-->

<div class="row cl">
    <label class="form-label col-xs-2 col-sm-2"><span class="c-red">*</span>商户编号：</label>
    <div class="formControls col-xs-10 col-sm-60">
        <select name="merchantNo" lay-search="" class="form-control input1" >
            <option value="">请选择</option>
            <option th:each="item:${equManager}"
                    th:value="${item.merchantNo}"
                    th:text="${item.merchantNo}" value="1">
            </option>
        </select>
    </div>
</div>




</body>
</html>

======================
package com.weilaidb.nice.controller;

import com.weilaidb.nice.test.Merchant;
import com.weilaidb.nice.test.ProdStat;
import com.weilaidb.nice.test.User;
import org.springframework.http.HttpRequest;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpSession;
import java.util.ArrayList;
import java.util.List;

@Controller
public class HelloController {
    @ResponseBody
    @RequestMapping("/hello")
    public String hello() {
        return "www.biancheng.net";
    }

    @RequestMapping("/testhello")
    public String test(Model model, HttpServletRequest request)
    {
        User user = new User();
        user.setFisrtName("123456789");
        user.setName("weilai");
        model.addAttribute("user",user);
        //创建session对象
        HttpSession session = request.getSession(true);
        session.setAttribute("user",user);//把用户数据保存到session对象中


        ProdStat stat = new ProdStat();
        stat.setCount(1000);
        model.addAttribute("prodStat",stat);

//        select option传递参数
        List<Merchant> list = new ArrayList<Merchant>();
        for (int i = 0;i < 10;i++) {
            Merchant merchant = new Merchant();
            merchant.setMerchantNo(i + 10);
            list.add(merchant);
        }
        model.addAttribute("equManager",list);



        return "demo";
    }
}



##需要创建Controller从指定templates下对应的哪个html文件
1.在templates下创建hello.html,内容仿照上面的
2.创建Controller，指定html的名称，示例2
3.传递参数使用HttpServletRequest


问题列表:
1./访问地址 报错
--html写的有问题
--原来就无法正常访问