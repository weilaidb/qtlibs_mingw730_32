一个JSP页面可由5种元素组合:
。普通的HTML标记
。JSP标记，如指令标记、动作标记。
。变量和方法的声明。
。JAVA程序片。
。JAVA表达式

原理:
当多个用户请求一个JSP页面时，JSP引擎为每个用户启动一个线程，该线程负责执行字节码文件响应用户的请求。JAVA程序片会被运行多次，在不同的线程中。


<%! %> 声明变量为全局变量
<%! %> 声明方法

<% %> JAVA程序片，局部变量

synchronized方法 互斥变量

