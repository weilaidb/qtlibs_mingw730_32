$ cat /etc/my.cnf$NL# For advice on how to change settings please see$NL# http://dev.mysql.com/doc/refman/8.0/en/server-configuration-defaults.html$NL$NL[mysqld]$NL#$NL# Remove leading # and set to the amount of RAM for the most important data$NL# cache in MySQL. Start at 70% of total RAM for dedicated server, else 10%.$NL# innodb_buffer_pool_size = 128M$NL#$NL# Remove the leading "# " to disable binary logging$NL# Binary logging captures changes between backups and is enabled by$NL# default. It's default setting is log_bin=binlog$NL# disable_log_bin$NL#$NL# Remove leading # to set options mainly useful for reporting servers.$NL# The server defaults are faster for transactions and fast SELECTs.$NL# Adjust sizes as needed, experiment to find the optimal values.$NL# join_buffer_size = 128M$NL# sort_buffer_size = 2M$NL# read_rnd_buffer_size = 2M$NL#$NL# Remove leading # to revert to previous value for default_authentication_plugin,$NL# this will increase compatibility with older clients. For background, see:$NL# https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_default_authentication_plugin$NL# default-authentication-plugin=mysql_native_password$NL$NLdatadir=\1/lib/mysql$NLsocket=\1/lib/mysql/mysql.sock$NL$NLlog-error=\1/log/mysqld.log$NLpid-file=\1/run/mysqld/mysqld.pid$NL$NL$NL$NL########--------------------------------$NLmkdir -p \1/lib/mysql$NLmkdir -p \1/log$NLmkdir -p \1/run/mysqld$NL########--------------------------------